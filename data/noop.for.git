This file has to be kept around to trick git into being willing to
create a data directory when it clones the repo.  This directory
is used by the BaliPlus program to store files containing subak data
(which should be excluded by .gitignore).
